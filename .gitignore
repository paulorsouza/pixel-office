# ---------- Build .NET ----------
**/bin/
**/obj/
*.user

# ---------- Dados de runtime ----------
# office.db é recriado e populado (seed) na primeira execução — apagar reseta tudo
*.db
*.db-shm
*.db-wal

# ---------- Logs ----------
livekit/*.log
**/Logs/*.png
**/Logs/*.log

# ---------- Unity ----------
# (office-unity tem o seu próprio .gitignore; isto aqui é reforço)
office-unity/Library/
office-unity/Temp/
office-unity/Logs/
office-unity/UserSettings/
office-unity/Build/
office-unity/Builds/
office-unity/*.csproj
office-unity/*.sln
office-unity/.vs/

# ---------- Node ----------
node_modules/
package-lock.json

# ---------- Assets ----------
# Os packs LimeZu crus (~1,6 GB) NÃO são versionados — ver ASSETS.md.
# Ficam FORA do repo (sibling Claude Sessions/LimeZu); se aparecer uma cópia
# dentro do repo, é ignorada aqui. São re-baixáveis do itch.io.
/LimeZu/
LimeZu/
# Os assets já recortados que o cliente carrega SÃO versionados (client-web/assets/).

# ---------- SO ----------
Thumbs.db
desktop.ini
.DS_Store
